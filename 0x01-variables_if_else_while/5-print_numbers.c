#include <stdio.h>

/**
 * main - print numbers all single digits of base 10
 *
 * Return: always 0 for success
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n";
	return (0);
}
