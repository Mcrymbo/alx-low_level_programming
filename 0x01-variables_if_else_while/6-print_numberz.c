#include <stdio.h>

/**
 * main - printing single digit numbers using char
 *
 * Return: always 0 for success
 */
int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
	putchar(i);
	}
	putchar('\n');
	return (0);
}	
