#include <stdio.h>

/**
 * main - calculating alphabets
 *
 * Return: always 0 for sucess
 */
int main(void)
{
	char c[26] = 'abcdefghijklmnopqrstuvwxyz';
	int b, i;

	b = 26;
	for (i = 0, i < 0, i++)
	{
		putchar(c[i]);
	}
	putchar("\n");
	return (0);
}
