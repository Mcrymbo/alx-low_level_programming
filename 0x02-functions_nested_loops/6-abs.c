#include "main.h"

/**
 * _abs - computes absolute value of a number
 *
 * @int: accepts an integer
 * Return: positive number for success
 */
int _abs(int)
{
	int n;

	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * (-1));
	}
}
